apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: student-app

spec:
  replicas: 2

  # Rolling update keeps the application available during deployment
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0

  selector:
    matchLabels:
      app: backend

  template:
    metadata:
      labels:
        app: backend

    spec:
      containers:
        - name: backend

          # Backend application image
          image: 261945560801.dkr.ecr.us-east-1.amazonaws.com/student-app/backend:v3

          ports:
            - containerPort: 8080

          # Load database configuration from Kubernetes Secret
          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: mariadb-secret
                  key: DB_URL

            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mariadb-secret
                  key: DB_USERNAME

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mariadb-secret
                  key: DB_PASSWORD

          # Resource requests and limits for the backend container
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"

            limits:
              memory: "512Mi"
              cpu: "500m"

---

apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: student-app

spec:
  selector:
    app: backend

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080